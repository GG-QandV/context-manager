context-manager/
├── src/
│   ├── index.ts                 # Entry point + migration
│   ├── app.ts                   # Fastify app setup
│   ├── config/
│   │   ├── index.ts             # Configuration (env-based)
│   │   ├── paths.ts             # Cross-platform path resolution
│   │   └── migration.ts         # Config migration (~/.iflow/ → new paths)
│   ├── services/
│   │   ├── postgres.service.ts  # PostgreSQL service
│   │   ├── qdrant.service.ts    # Qdrant vector DB service
│   │   ├── embedding.service.ts # Embedding generation (TEI / OpenAI / NoOp)
│   │   ├── timeParser.service.ts# Natural language time parsing
│   │   └── contentDetector.service.ts  # Content type detection
│   ├── routes/
│   │   ├── health.routes.ts     # Health check
│   │   ├── context.routes.ts    # Main context CRUD + config endpoints
│   │   ├── search.routes.ts     # Full-text, semantic, hybrid search
│   │   └── sync.routes.ts       # Sync operations
│   ├── schemas/
│   │   └── context.schema.ts    # TypeBox schemas
│   ├── middleware/
│   │   └── error-handler.ts     # Global error handling
│   └── types/
│       └── index.ts             # TypeScript types + shared interfaces
├── scripts/
│   ├── install.sh               # Linux/macOS installer
│   ├── install.ps1              # Windows installer (PowerShell)
│   ├── uninstall.sh             # Linux/macOS uninstaller
│   ├── uninstall.ps1            # Windows uninstaller (PowerShell)
│   └── init-mcp-config.mjs      # MCP config generator
├── mcp/
│   └── server.js                # MCP stdio server
├── mcp.json                     # MCP client config (dev)
├── mcp.json.template            # MCP config template for installer
├── resync_qdrant.py             # Qdrant resync utility (Python)
├── Dockerfile
├── docker-compose.yml
├── package.json
├── tsconfig.json
└── .env.example
